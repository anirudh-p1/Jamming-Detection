# Jamming Detection
# An RF Spectogram Autoencoder

You can't train a supervised classifier on attack types you haven't seen. Autoencoder trained on clean spectrograms only, reconstruction error as anomaly score.
